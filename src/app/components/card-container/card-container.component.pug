include ../../../mixins

button(type="button")
    +icon('icon-add')
section.card-container
    card(
        *ngFor="let item of itemsList",
        [card]="item",
        [owner-menu]="ownerMenu"
    )
