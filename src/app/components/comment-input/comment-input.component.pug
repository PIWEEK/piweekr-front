avatar-editor(
    editable="false", 
    avatar-type="medium-head", 
    [avatar]="session.currentUser.avatar")
  
form(
  method="post",
  (ngSubmit)="onSubmit()"
)
  input(type="text", placeholder="Introduce un comentario", [(ngModel)]="comment", name="comment")
  button(type="submit") Enviar