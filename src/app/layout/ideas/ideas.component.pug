include ../../../mixins

vertical-list(
    [type]="type",
    [items]="items",
    (currentItem)="handleSelected($event)"
)
    +comploader
card-container(
    entity="ideas",
    [type]="type",
    [items]="items",
    [selectedItem]="selectedItem",
    [owner-menu]="ownerMenu"
)
    +comploader
